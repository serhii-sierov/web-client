import { query } from '@/src/lib/apollo/client';
import { auth } from '@/src/lib/auth';
import { gql } from '@apollo/client';

const Home = async () => {
  return (
    <div>
      <main>Home</main>
    </div>
  );
};

export default Home;
